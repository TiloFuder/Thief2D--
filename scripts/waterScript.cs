using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class waterScript : StimAndResponse
{
    // Start is called before the first frame update
    
    public waterScript() { }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
